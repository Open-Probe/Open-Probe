langchain==0.3.24
langchain-core==0.3.56
langgraph==0.2.76
langchain-openai
openai
# Verifies Sonic's private internal CA chain against the OS trust store.
# Arrives transitively via crawl4ai today; pinned here so it cannot vanish.
truststore>=0.9
python-dotenv
loguru

